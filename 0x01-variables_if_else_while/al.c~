#include <stdlib.h>
#include <stdio.h>
/**
 * main - dont print  and e
 * Return: always ziro
 */
int main(void)
{
char a;
for (a = 'a'; a <= 'z'; a++)
{
while ((a != 'q') && (a != 'a'))
{
putchar (a)
}
}
}
