#include <stdio.h>
/**
 * main - return name of the programe
 * @agrc: argument count
 * @agrv: argument vector
 * Return: always zero
 */
int main(int agrc, char *agrv[])
{
(void)agrc;
printf("%s\n", agrv[0]);
return (0);
}
