#include <stdio.h>

/**
 * main - printing the number of argvs
 * @argv: argument vector
 * @argc: argument count
 *
 * Return: always zero
 */
int main(int argc, char *argv[])
{
(void)argv;
printf("%d\n", argc);
return (0);
}
